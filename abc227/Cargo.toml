[package]
name = "abc227"
version = "0.1.0"
edition = "2021"

# dependencies added to new project
[dependencies]
proconio = { version = "0.3.8", features = ["derive"] }
itertools = "0.9.0"
num-rational = "0.2.4"

[profile.release]
lto = true
panic = 'abort'
