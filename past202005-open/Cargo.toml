[package]
name = "past202005-open"
version = "0.1.0"
edition = "2021"

# dependencies added to new project
[dependencies]
proconio = { version = "0.3.8", features = ["derive"] }

[profile.release]
lto = true
panic = 'abort'
