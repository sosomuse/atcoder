use proconio::input;

fn main() {
    input! {
        n: usize,
        // k: usize,
        // p: [usize; n],
    };

    // let vec: Vec<isize> = vec![-1; n];
    // let vec2: Vec<Vec<usize>> = vec![];
    // let min = (0, 0);

    // for i in 0..n {}

    println!("{}", n);
}
