[package]
name = "abc335"
version = "0.1.0"
edition = "2021"

# dependencies added to new project
[dependencies]
proconio = { version = "0.4.5", features = ["derive"] }
itertools = "0.11.0"

[profile.release]
lto = true
panic = 'abort'
