use proconio::input;

fn main() {
    input! {
        n: usize,
        k: usize,
        p: [usize; n],
    };

    let vec: Vec<usize> = vec![0; n];
    let vec2: Vec<Vec<usize>> = vec![];

    for i in 0..n {}

    println!("{}", n);
}
