[package]
name = "abc257"
version = "0.1.0"
edition = "2021"

# dependencies added to new project
[dependencies]
proconio = { version = "0.3.8", features = ["derive"] }
superslice = { version = "1.0.0" }

[profile.release]
lto = true
panic = 'abort'
